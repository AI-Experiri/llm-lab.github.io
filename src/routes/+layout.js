// This enables static prerendering for all pages
export const prerender = true;

// Generate /route/index.html instead of /route.html for proper static hosting
export const trailingSlash = 'always';
